# Re-coupled to the release number at 0.49.0: this stamp is what package.py
# writes into every pack manifest ("packed by Lot ..."), and it had sat at
# 0.18.0 while the VERSION file reached 0.48.0 -- so no pack since then has
# been traceable to the Lot that actually packed it. Same re-coupling
# deli_counter's KIT_VERSION got at 0.96.0.
LOT_VERSION = "0.49.0"
